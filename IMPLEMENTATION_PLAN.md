# ShiftWeave 实施方案

## 一、项目定位

ShiftWeave 是一个以 MoonBit 编写的离线排班与局部重排引擎。它将人员、技能、班次和规则转换为约束满足问题，通过约束传播与启发式搜索生成排班，并用可解释的冲突证据说明无解原因。

项目的差异化核心：

- 可解释约束求解，而不是只返回成功或失败；
- 同时考虑硬约束、偏好和人员公平性；
- 请假或需求变化后执行最小扰动重排；
- 核心库与 CLI 分离，可被其他 MoonBit 项目复用。

## 二、功能边界

### 输入

- 人员、技能和最大/目标工时；
- 可用时间、不可用时间和班次偏好；
- 班次时间、人数及技能需求；
- 最短休息时间、最大连续班次等配置规则；
- 可选的既有排班，用于局部重排。

### 输出

- 可执行的排班结果；
- 未覆盖班次和冲突原因；
- 人员工时、公平性和偏好满足统计；
- Markdown、CSV、JSON 报告。

### 不在范围内

- 不开发在线账户、考勤或薪资系统；
- 不提供特定地区劳动法规判断；
- 不接入人脸、定位等个人信息采集能力；
- 不承诺求得理论全局最优，只保证结果可验证并报告求解状态。

## 三、技术架构与代码规模

| 模块 | 内容 | 预计有效 MoonBit 行数 |
|---|---|---:|
| domain | 人员、技能、时间段、班次、分配、规则模型 | 450–600 |
| input | CSV/JSON 解析、字段诊断、规范化 | 600–800 |
| constraint | 硬约束、软约束、候选域和冲突证据 | 750–950 |
| propagation | 域削减、强制分配、前向检查 | 550–750 |
| solver | 启发式回溯、分支排序、预算与求解状态 | 900–1,200 |
| scoring | 工时均衡、偏好、轮换和连续班评分 | 450–650 |
| repair | 请假、换班、最小扰动局部重排 | 500–700 |
| report_cli | CLI、摘要、Markdown/CSV/JSON 报告 | 600–800 |
| 合计 | 不含测试、示例和文档 | 4,800–6,450 |

## 四、关键算法

1. 为每个班次岗位建立候选人员域，先过滤不可用时间和技能不匹配人员；
2. 使用约束传播处理唯一候选、容量不足、时间重叠和休息间隔；
3. 采用“最少候选优先”选择待分配岗位，降低搜索分支；
4. 对候选人按照当前工时、偏好、连续班次和公平性增量排序；
5. 搜索过程中维护冲突证据，并在失败时汇总最小化的可解释原因；
6. 局部重排时冻结未受影响分配，将相对原排班的改动数量和代价加入目标函数。

## 五、测试与验收

- `moon check`、`moon test`、`moon fmt --check` 全部通过；
- 单元测试覆盖时间段、约束、传播、评分和报告；
- 端到端测试覆盖正常排班、技能缺口、人数不足、不可用冲突、无解和请假重排；
- 对每个输出方案运行独立验证器，确保不存在未报告的硬约束违规；
- 提供门店、实验室和志愿活动三组示例；
- GitHub Actions 在 push 和 pull request 时执行构建、测试、格式检查与示例冒烟测试；
- README 包含安装、配置、运行、输出解释和故障排查；
- 使用 Apache-2.0，并记录所有第三方依赖及其许可证；
- 完成 Mooncakes dry-run 后发布正式版本。

## 六、真实提交规划

至少形成以下 8 个可追踪、具有独立意义的提交：

1. 初始化 MoonBit 工程、许可证、README 骨架和 CI；
2. 实现领域模型、时间段运算和基础验证；
3. 实现 CSV/JSON 输入、规范化和错误诊断；
4. 实现硬约束、候选域和独立方案验证器；
5. 实现约束传播和启发式回溯求解；
6. 实现公平性、偏好及多方案评分；
7. 实现请假、换班和最小扰动局部重排；
8. 完成 CLI、报告、示例、测试、文档和 Mooncakes 发布配置。

## 七、重复度与原创性控制

公开扫描中较拥挤的 MoonBit 方向包括解释器、LSP、Shell、HTTP/代理、路径规划、OpenTelemetry、证据存证、版权合规、音频、图布局、数据库和 OCI 工具。ShiftWeave 聚焦“排班约束求解、无解解释与最小扰动重排”，与这些项目的功能边界明显不同。

公开渠道未发现高度相似的 MoonBit 排班项目，但未公开的报名项目无法被检索。开发期间将继续在 GitHub、Mooncakes 和赛事公开作品中按 `schedule`、`roster`、`shift`、`constraint solver` 等关键词复查；若发现撞题，将优先强化局部重排与冲突解释能力，而不是复制对方功能。
